import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
	palette: {
		primary: { main: '#00ACC1' },
		secondary: { main: '#81D4FA' }
	},

//const themeName = 'pacific Blue Malibu Vulture';
})

export default theme;